openapi: 3.0.0
info:
  title: Looply API Gateway
  version: 1.0.0
  description: API Gateway for Looply services

servers:
  - url: http://localhost:3000
    description: Development server

paths:
  /health:
    get:
      summary: Health check for Api Gateway
      tags:
        - Api Gateway
      responses:
        "200":
          description: Api Gateway is healthy
          content:
            application/json:
              example:
                status: ok